import { Box } from "@chakra-ui/react";
import { memo } from "react";

export const Header = memo(() => {
	return (
		<>
			<Box>“ NoA -novel aidor- ”</Box>
		</>
	);
});

Header.displayName = "Header";
